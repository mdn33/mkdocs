site_name: Reply Docs
theme:
  name: readthedocs
  highlightjs: true
extra:
  hide_footer: true
plugins:
- search
- mkdocstrings:
    default_handler: python
      handlers:
        python:
          paths: [src]
